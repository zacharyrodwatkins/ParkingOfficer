#!/usr/bin/env python

#from rospy.std_msgs.msg import String
#from rospy.geometry_msgs.msg import Twist

#from tensorflow import keras
#import keras
import csv
import os
#import rospy
import sys
print("Using tensorflow backend.")

NUM_LOCATIONS = 8

class DirtyCheat():
  

    def __init__(self):
    
        self.plates = []

        # Populate tables
        LICENSE_PLATE_FILE = '/home/zack/enph353_ws/src/2019F_competition_students/enph353/enph353_gazebo/scripts/plates.csv'
        SCRIPT_FILE_PATH = os.path.dirname(os.path.realpath(__file__))
        with open(LICENSE_PLATE_FILE, "r") as plate_file:
            platereader = csv.reader(plate_file)
            i=0
            for row in platereader:
                if i < NUM_LOCATIONS:
                    self.plates.append(row[0])
                else:
                    break
                i += 1



    def getPlates(self):
        return self.plates





























































































































































































































































































































































































if __name__ =="__main__":
    c = DirtyCheat()
    print(c.getPlates())